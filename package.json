{
  "name": "homelab-apps",
  "version": "0.1.0",
  "private": true,
  "description": "Personal homelab app deployments — Pulumi stacks consuming @mrsimpson/homelab-core-components",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "type-check": "npm run type-check --workspaces --if-present"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.7.0"
  }
}
